# ===========================================
# ProductQ Server Configuration
# ===========================================

# LLM Provider (legacy - now using presets on frontend)
# Kept for backwards compatibility
LLM_PROVIDER=gemini

# ===========================================
# Google/Gemini Settings
# ===========================================
# Required if using 'gemini-cloud' or 'gemma-cloud' presets
# Option 1: Single key
GOOGLE_API_KEY=your_google_api_key_here

# Option 2: Multiple keys with labels (format: label:key,label:key)
# Keys are rotated automatically when rate limited (30s cooldown per key)
# GOOGLE_API_KEYS=mykey:AIza...,backup:AIza...

# ===========================================
# Groq Settings (for 'best-free-cloud' preset)
# ===========================================
# Server-side fallback keys used when users don't provide their own
# Get keys from https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key_here

# Multiple keys with labels (format: label:key,label:key)
# GROQ_API_KEYS=main:gsk_...,backup:gsk_...

# ===========================================
# Ollama Settings (for 'local-llm' preset)
# ===========================================
# OLLAMA_HOST is the standard env var used by Ollama CLI
OLLAMA_HOST=http://localhost:11434
OLLAMA_LLM_MODEL=qwen2.5-coder:3b
OLLAMA_EMBEDDING_MODEL=nomic-embed-text
